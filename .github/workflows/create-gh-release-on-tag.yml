name: Create GitHub Release on tag

on:
  push:
    tags:
      - "v[0-9]*.[0-9]*.[0-9]*"

jobs:
  release:
    permissions:
      contents: write
    uses: ./.github/workflows/create-gh-release-from-changelog.yml
